# robots.txt for https://jamesccupps.github.io/
# Allow all compliant crawlers by default.

User-agent: *
Allow: /

# GitHub Pages serves every file in a project repo, so the raw README.md of
# each project site (e.g. /Wetbulb/README.md, /CoolingTowerSim/README.md) is
# publicly fetchable as text/markdown. The rendered app pages are the
# canonical content, so keep the raw markdown from competing with them in
# search results.
Disallow: /*.md$

Sitemap: https://jamesccupps.github.io/sitemap.xml
